# Builds TrayHider.exe directly from TrayHider.ahk on a Windows GitHub-hosted
# runner every time a version tag (e.g. v1.0.0) is pushed, then attaches the
# compiled .exe and its SHA256 checksum to a GitHub Release.
#
# Why this matters: anyone can open the "Actions" tab for a given release and
# see the exact, unmodified build log — the binary is provably built from the
# source in this repo, not compiled privately and uploaded blind.

name: Build and Release

on:
  push:
    tags:
      # Matches both "v1.0.0" and "V1.0.0" — GitHub's own release UI
      # sometimes capitalizes the tag depending on what you type into the
      # "Choose a tag" box, and this pattern is case-sensitive by default,
      # so both cases are listed explicitly to avoid silently missing builds.
      - "v*"
      - "V*"
  # Lets you trigger a build manually from the Actions tab (with a fake/blank
  # tag input) — useful to confirm GitHub even recognizes this workflow file
  # without needing to create a real git tag every time you want to test it.
  workflow_dispatch:
    inputs:
      release_tag:
        description: "Tag to attach this build to (must already exist, e.g. v1.1.0). Leave blank to just test the build without creating/updating a release."
        required: false
        default: ""

permissions:
  contents: write

jobs:
  build:
    runs-on: windows-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Download AutoHotkey v2 + Ahk2Exe (official GitHub releases)
        run: |
          # Ask the GitHub API for the latest release of each official repo and
          # grab its .zip asset — no hardcoded URLs or versions to go stale.
          $ahkRel  = Invoke-RestMethod "https://api.github.com/repos/AutoHotkey/AutoHotkey/releases/latest"
          $ahkZip  = $ahkRel.assets  | Where-Object { $_.name -like "*.zip" } | Select-Object -First 1
          $cplRel  = Invoke-RestMethod "https://api.github.com/repos/AutoHotkey/Ahk2Exe/releases/latest"
          $cplZip  = $cplRel.assets  | Where-Object { $_.name -like "*.zip" } | Select-Object -First 1

          Write-Host "AutoHotkey asset : $($ahkZip.name)"
          Write-Host "Ahk2Exe asset    : $($cplZip.name)"

          Invoke-WebRequest $ahkZip.browser_download_url -OutFile "ahk.zip"
          Invoke-WebRequest $cplZip.browser_download_url -OutFile "ahk2exe.zip"
          Expand-Archive "ahk.zip"     -DestinationPath "ahk"
          Expand-Archive "ahk2exe.zip" -DestinationPath "ahk2exe"
        shell: pwsh

      - name: Compile TrayHider.ahk to TrayHider.exe
        run: |
          $ahk2exe = Get-ChildItem -Path "ahk2exe" -Recurse -Filter "Ahk2Exe.exe" | Select-Object -First 1
          $base    = Get-ChildItem -Path "ahk"     -Recurse -Filter "AutoHotkey64.exe" | Select-Object -First 1

          if (-not $ahk2exe) {
            Get-ChildItem -Path "ahk2exe" -Recurse | Select-Object FullName | Out-String | Write-Host
            Write-Error "Ahk2Exe.exe not found in the downloaded compiler package."
            exit 1
          }
          if (-not $base) {
            Get-ChildItem -Path "ahk" -Recurse | Select-Object FullName | Out-String | Write-Host
            Write-Error "AutoHotkey64.exe not found in the downloaded AutoHotkey package."
            exit 1
          }

          Write-Host "Compiler : $($ahk2exe.FullName)"
          Write-Host "Base     : $($base.FullName)"

          # Embed the custom icon if icon.ico exists in the repo root
          $iconArgs = @()
          if (Test-Path "icon.ico") {
            Write-Host "Icon     : $PWD\icon.ico"
            $iconArgs = @("/icon", "`"$PWD\icon.ico`"")
          } else {
            Write-Host "Icon     : (none found — building with default AHK icon)"
          }

          # /silent is required on a headless runner — otherwise Ahk2Exe may
          # try to open a GUI dialog nobody can click and the job hangs.
          Start-Process -FilePath $ahk2exe.FullName -Wait -NoNewWindow -ArgumentList (@(
            "/silent", "verbose",
            "/in",   "`"$PWD\TrayHider.ahk`"",
            "/out",  "`"$PWD\TrayHider.exe`"",
            "/base", "`"$($base.FullName)`""
          ) + $iconArgs)

          # Ahk2Exe may return before the file is flushed; wait briefly
          $tries = 0
          while (!(Test-Path "TrayHider.exe") -and $tries -lt 10) {
            Start-Sleep -Seconds 1
            $tries++
          }
        shell: pwsh

      - name: Verify the .exe was actually produced
        run: |
          if (!(Test-Path "TrayHider.exe")) {
            Write-Error "Compilation failed: TrayHider.exe was not created."
            exit 1
          }
        shell: pwsh

      - name: Build installer with Inno Setup
        run: |
          # windows-latest runners ship with Inno Setup 6 preinstalled at
          # this path. Fall back to installing it via Chocolatey if it's
          # ever missing, so this step doesn't silently do nothing.
          $iscc = "C:\Program Files (x86)\Inno Setup 6\ISCC.exe"
          if (!(Test-Path $iscc)) {
            Write-Host "ISCC.exe not found at the expected path — installing Inno Setup via choco."
            choco install innosetup -y --no-progress
            $iscc = "C:\Program Files (x86)\Inno Setup 6\ISCC.exe"
          }
          if (!(Test-Path $iscc)) {
            Write-Error "Inno Setup compiler still not found after install attempt."
            exit 1
          }

          & $iscc "TrayHider.iss"

          $built = Get-ChildItem -Path "installer_output" -Filter "*.exe" -ErrorAction SilentlyContinue | Select-Object -First 1
          if (-not $built) {
            Write-Error "Installer build did not produce an .exe in installer_output\."
            exit 1
          }
          Copy-Item $built.FullName -Destination "TrayHider-Setup.exe"
        shell: pwsh

      - name: Generate SHA256 checksums
        run: |
          foreach ($f in @("TrayHider.exe", "TrayHider-Setup.exe")) {
            $hash = Get-FileHash -Path $f -Algorithm SHA256
            "$($hash.Hash)  $f" | Out-File -FilePath "$f.sha256.txt" -Encoding ascii -Append:$false
            Get-Content "$f.sha256.txt"
          }
        shell: pwsh

      # Always uploaded regardless of trigger type, so a manual test run
      # (workflow_dispatch with no tag) still lets you download the built
      # files straight from this run's Summary page — useful for confirming
      # the build itself works without needing to publish a Release yet.
      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: TrayHider-build
          path: |
            TrayHider.exe
            TrayHider.exe.sha256.txt
            TrayHider-Setup.exe
            TrayHider-Setup.exe.sha256.txt
          retention-days: 30

      - name: Create GitHub Release
        # Only attach to a Release when this run came from an actual tag
        # push, or from a manual run where a release_tag was explicitly
        # provided. A manual "just test the build" run (blank input) skips
        # this step and relies on the artifact upload above instead.
        if: github.event_name == 'push' || (github.event_name == 'workflow_dispatch' && github.event.inputs.release_tag != '')
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.event_name == 'push' && github.ref_name || github.event.inputs.release_tag }}
          files: |
            TrayHider.exe
            TrayHider.exe.sha256.txt
            TrayHider-Setup.exe
            TrayHider-Setup.exe.sha256.txt
            TrayHider.ahk
          body: |
            Automated build compiled directly from the source in this repository
            by GitHub Actions — see the Actions tab for the full build log.

            **Two ways to install:**
            - `TrayHider-Setup.exe` — installer, no admin/UAC prompt (installs
              to your user profile, not Program Files), with an option to
              enable auto-start during setup.
            - `TrayHider.exe` — portable, just run it directly, no install.

            **Before running:** if your antivirus flags either file, this is
            a known false positive common to compiled AutoHotkey scripts — see
            the README for an explanation and how to verify the binaries or
            build them yourself.

            Verify a download with:
            ```powershell
            certutil -hashfile TrayHider.exe SHA256
            ```
            and compare against the matching `.sha256.txt` file.
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
